import e from "controller/index.js"
export default {
  getData: {
    method: e.type == 1 ? "GET" : "GET",
    url: e.base + "/operandrole/listpage.json"
  },
  getByIdData: {
    method: e.type == 1 ? "GET" : "POST",
    url: e.base + "/operandrole/add.json"
  },
  saveEdit: {
    method: e.type == 1 ? "POST" : "POST",
    url: e.base + "/operandrole/save.json"
  },

};
